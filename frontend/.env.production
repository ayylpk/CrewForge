# 生产构建（vite build 自动加载，mode=production）
# 空值 = 同源相对路径：请求打 /api/... ，由站点自己的 nginx 反代到后端
# （见 deploy/nginx.conf 的 location /api/）。这样换端口/域名/容器 IP 都不用改代码，
# 也绝不会像写 http://localhost:8080 那样让访客的浏览器去访问访客自己的电脑。
VITE_API_BASE=
