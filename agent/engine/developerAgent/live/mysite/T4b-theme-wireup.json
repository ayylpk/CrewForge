{
  "type": "architect_task",
  "projectId": "mysite",
  "taskId": "mysite-T4b-theme-wireup",
  "requirementSnapshot": {
    "goal": "T4 收尾手术：只差一行——main.ts 未引入 src/styles/theme.css。验收脚本其余断言（4 视图/路由/Home 视觉/App 导航/title）上一轮已全部通过。",
    "context": "现状已实测：verify-t4 只在「main.ts 未引入全局主题样式」一处红。theme.css 已存在且内容完整（含 --sky/--sun/--grass 变量）。"
  },
  "stackProfile": {
    "frontend": "Vue3 + Vite + TS（不动依赖）",
    "backend": "不动",
    "database": "不动"
  },
  "domainModel": { "entity": "(无)" },
  "contract": { "version": "1", "endpoints": [] },
  "foundationPlan": {
    "dirs": ["frontend"],
    "workItems": [
      { "id": "w1-wireup", "kind": "frontend", "title": "main.ts 挂接 theme.css，其余一律不动", "paths": ["frontend/src/main.ts"] }
    ]
  },
  "allowedRoots": ["backend", "frontend"],
  "forbiddenPaths": ["backend/mvnw", "backend/mvnw.cmd", "backend/.mvn", "scripts"],
  "acceptanceChecks": [
    { "id": "visual-contract", "command": "node", "args": ["../scripts/verify-t4.mjs"], "cwd": "frontend", "timeoutMs": 60000, "category": "RENDER" },
    { "id": "frontend-build", "command": "npm", "args": ["run", "build"], "cwd": "frontend", "timeoutMs": 300000, "category": "COMPILE" }
  ],
  "developerInstructions": "外科手术，最小改动：readFile frontend/src/main.ts 看现状 → editFile 在 createApp/router 装配之前插入 import './styles/theme.css'（若已有类似行但路径不对则改正）。除 main.ts 外**任何文件都不许动**（上轮已验收通过的部分动了就前功尽弃）。改完直接 done——验收只跑这两条。预算宝贵，一步到位，别 inspect 全树。"
}
