# MySQL 资产约束

- 默认字符集为 utf8mb4，时区统一为 UTC。
- 数据库名、用户和密码来自环境变量，模板不得包含真实密码。
- 表名和字段只从 Domain Model 渲染，禁止模型自行切换单复数。
- 结构变化必须新增版本化迁移，禁止运行时隐式改表。
- 应用账号不使用 root，生产环境不得暴露数据库端口。
- 所有外键、唯一性和非空约束必须与 Domain Model 一致。
- 删除策略必须由需求明确；不默认级联删除。
- `compose.mysql.yml` 仅作为开发环境资产，生产部署需要独立配置。
