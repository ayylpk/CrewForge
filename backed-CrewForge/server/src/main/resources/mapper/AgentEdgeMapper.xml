<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hina.crewforge.mapper.AgentEdgeMapper">

    <select id="listByAgentId" resultType="com.hina.crewforge.pojo.entity.AgentEdge">
        SELECT id, agent_id, from_node, type, to_nodes, create_time, update_time
        FROM sys_agent_edge
        WHERE agent_id = #{agentId}
          AND deleted = 0
        ORDER BY id ASC
    </select>

    <insert id="insert" parameterType="com.hina.crewforge.pojo.entity.AgentEdge"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_agent_edge (
            agent_id,
            from_node,
            type,
            to_nodes,
            create_time,
            update_time
        ) VALUES (
            #{agentId},
            #{fromNode},
            #{type},
            #{toNodes},
            #{createTime},
            #{updateTime}
        )
    </insert>

    <update id="updateById" parameterType="com.hina.crewforge.pojo.entity.AgentEdge">
        UPDATE sys_agent_edge
        SET from_node = #{fromNode},
            type = #{type},
            to_nodes = #{toNodes},
            update_time = #{updateTime}
        WHERE id = #{id}
          AND deleted = 0
    </update>

    <select id="getById" resultType="com.hina.crewforge.pojo.entity.AgentEdge">
        SELECT id, agent_id, from_node, type, to_nodes, create_time, update_time
        FROM sys_agent_edge
        WHERE id = #{id}
          AND deleted = 0
    </select>

    <update id="deleteById">
        UPDATE sys_agent_edge
        SET deleted = 1,
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <update id="deleteByAgentIds">
        UPDATE sys_agent_edge
        SET deleted = 1,
            update_time = #{updateTime}
        WHERE agent_id IN
        <foreach collection="agentIds" item="agentId" open="(" separator="," close=")">
            #{agentId}
        </foreach>
          AND agent_id IN (
              SELECT id FROM sys_agent WHERE user_id = #{userId} AND deleted = 0
          )
    </update>

</mapper>
