<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hina.crewforge.mapper.TenantMapper">

    <select id="list" parameterType="com.hina.crewforge.pojo.QueryParam.TenantQueryParam"
            resultType="com.hina.crewforge.pojo.entity.Tenant">
        <bind name="offset" value="(page - 1) * pageSize"/>
        SELECT id, name, owner_id, contact, phone, status, invitation_code, max_members, create_time, update_time
        FROM sys_tenant
        WHERE deleted = 0
        <if test="tenantName != null and tenantName != ''">
            AND name LIKE CONCAT('%', #{tenantName}, '%')
        </if>
        ORDER BY create_time DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

</mapper>
