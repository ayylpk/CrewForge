<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hina.crewforge.mapper.AgentPoolMapper">

    <select id="list" parameterType="com.hina.crewforge.pojo.QueryParam.AgentPoolQueryParam"
            resultType="com.hina.crewforge.pojo.entity.AgentPool">
        <bind name="offset" value="(page - 1) * pageSize"/>
        SELECT id, user_id, name, role, system_prompt, tools, model, temperature, status, create_time, update_time
        FROM sys_agent
        WHERE deleted = 0
        <if test="userId != null">
            AND user_id = #{userId}
        </if>
        <if test="keyword != null and keyword != ''">
            AND (name LIKE CONCAT('%', #{keyword}, '%') OR role LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        ORDER BY create_time DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <insert id="insert" parameterType="com.hina.crewforge.pojo.entity.AgentPool"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_agent (
            user_id,
            name,
            role,
            system_prompt,
            tools,
            model,
            temperature,
            status,
            create_time,
            update_time
        ) VALUES (
            #{userId},
            #{name},
            #{role},
            #{systemPrompt},
            #{tools},
            #{model},
            #{temperature},
            #{status},
            #{createTime},
            #{updateTime}
        )
    </insert>

    <update id="updateById" parameterType="com.hina.crewforge.pojo.entity.AgentPool">
        UPDATE sys_agent
        SET name = #{name},
            role = #{role},
            system_prompt = #{systemPrompt},
            tools = #{tools},
            model = #{model},
            temperature = #{temperature},
            status = #{status},
            update_time = #{updateTime}
        WHERE id = #{id}
          AND deleted = 0
    </update>

    <select id="getById" resultType="com.hina.crewforge.pojo.entity.AgentPool">
        SELECT id, user_id, name, role, system_prompt, tools, model, temperature, status, create_time, update_time
        FROM sys_agent
        WHERE id = #{id}
          AND deleted = 0
    </select>

    <update id="deleteByIds">
        UPDATE sys_agent
        SET deleted = 1,
            update_time = #{updateTime}
        WHERE user_id = #{userId}
          AND id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

</mapper>
