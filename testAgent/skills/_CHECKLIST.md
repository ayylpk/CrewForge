# SKILL 自查清单（Anthropic 7 实践 → testAgent 落地版）

> 写第二、第三个 skill 时逐条过。每条 = 问题 → 根因 → 方案；**第 0 条是元要求：正文每条规范必须能追溯到一次真实发生的失败**（RED 实录），凭空列的禁止项不要写。

| # | 检查项 | 过线标准 | 本仓先例 |
|---|---|---|---|
| 0 | 每条规范有出处 | 红线/借口都能指到 evals/results 里的实录 | f4 三次作弊 → 三条红线 |
| 1 | 自由度收窄 | 判断类输出带 0-100 置信度 + ≥80 阈值 + 误报排除清单 | 报告契约 |
| 2 | description 合格 | 纯中文（本项目语境）/第三人称/what 一短语+when 触发词+**排除条件**（触发决策在 L1，排除必须进 L1，不能留在正文）；与红线精神冲突的词不放 | 复盘问题①②：`让测试变绿`已移出 |
| 3 | 渐进披露 | 正文 ≤500 行（目标 ≤100）；长内容外置 | SKILL.md ~70 行 |
| 4 | 引用一层 + TOC | reference 只从 SKILL.md 直链；>100 行加目录；**加载时机写成可观察谓词**（"按测试命令查对应节，只读那节"，不是"首次接触时"） | references/test-logs.md |
| 5 | 评测先行 | 先裸跑建基线（≥3 卷，fresh session，考卷注释**严禁泄题**）；机械断言=source of truth；改 skill 必重跑 | evals/ 4 卷+字节比对 |
| 6 | 跨档验证 | 每个计划档位至少跑关键卷；**纪律/红线类专测最弱档**（越弱越会作弊） | `DEFAULT_MODEL=deepseek-v4-pro bun run evals/run.ts f4-broken` |
| 7 | 退休分类 | 补能力型（随模型变强退役）vs 偏好型（长期）；红线型=偏好型，退休压力小 | 本 skill=偏好型 |
| 8 | 预算/环境告知 | 有限迭代/输出约束要显式给模型（它不知道"还剩几轮"） | main.ts 75% 提醒 |

**跑法**：改完 SKILL.md → `bun x tsc --noEmit` → `bun run evals/run.ts`（flash 全套）→ 关键卷加跑 pro 档 → 更新记忆 → commit。
